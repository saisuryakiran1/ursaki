# UrSaKi infrastructure environment template
# Copy to project root: cp infra/.env.docker infra/.env (or merge into root .env.example values)

COMPOSE_PROJECT_NAME=ursaki
BACKEND_PORT=8000
OLLAMA_PORT=11434
CHROMA_PORT=8001
